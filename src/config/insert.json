{
    "id" : "mosip.abis.insert",
    "version" : "1.1",
    "requestId" : "${requestId}",
    "requesttime" : "${requesttime}",
    "referenceId" : "${referenceId}",
    "referenceURL" : "${referenceURL}"
}
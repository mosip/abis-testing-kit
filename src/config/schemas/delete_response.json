{
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "id": {
            "type": "string",
            "pattern": "^mosip.abis.delete$"
        },
        "requestId": {
            "type": "string"
        },
        "timestamp" : {
            "type": "string",
            "pattern":  "^\\d*$"
        },
        "returnValue" : {
            "type": "integer",
            "minimum":  0,
            "maximum": 9
        }
    },
    "required": ["id", "requestId", "timestamp", "returnValue"]
}